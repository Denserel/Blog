@model Post


<div>
    @if (!String.IsNullOrEmpty(Model.Image))
    {
        var path = $"/image/{Model.Image}";
        <span class="title"> @Model.Title</span>
        <img src="@path">
    }
</div>
<div class="post-body">
    @Model.Body
</div>
